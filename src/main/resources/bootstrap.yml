spring:
  application:
    name: servicio-productos
  cloud:
    config:
      uri: http://localhost:8888
    
management:
  endpoints:
    web:
      exposure:
        include: "*"

---

spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888        
